<div class='card z-depth-0 pink lighten-5'>
    <div class='card-content'>
        <div class='row'>
            <div class='col m3'>
                {% if review.author.avatar is not null %}
                    <div class="valign-wrapper">
                        <div class="valign">
                            <div class="circle" style="width: 100px; height: 100px; overflow: hidden;">
                                <img src="{{ vich_uploader_asset(review.author.avatar, 'file') | imagine_filter('profile_thumbnail') }}" alt="" class="circle responsive-img" >
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
            <div class='col m9'>
                <h3 class='author-article'>
                    <a class='pink-text lighten-2' href='{{ path('shiawa_user_view', {'username' : review.author.username }) }}'>
                        {{ review.author.username }}
                    </a>
                </h3>
                {% if review.author.description is not empty %}
                    <p>{{ review.author.description|raw }}</p>
                {% else %}
                    <p>Pas de description de l'auteur</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>